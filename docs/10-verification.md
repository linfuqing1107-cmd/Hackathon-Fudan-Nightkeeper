# v0.2 本地验证记录

日期：2026-09-12。环境：macOS arm64、Node v25.8.2、Next.js 16.3.5、PGlite 0.5.8。依赖确切版本见package-lock.json。CI目标Node 22，远程结果尚未验证。

| 检查 | 实测结果 |
| --- | --- |
| TypeScript | npm run typecheck通过 |
| 单元/契约/数据库 | npm test，3个测试文件、18个测试通过 |
| 生产构建 | npm run build通过，页面静态生成、API动态执行 |
| 生产模式浏览器/接口 | npm run test:e2e，4个测试通过 |
| 依赖审计 | npm audit，当前报告0 vulnerabilities，不代表绝对安全 |
| 桌面检查 | 1440x1080截图已查看；趋势及任务正常显示 |
| 手机检查 | 390x844截图已查看；根页面无横向溢出，患者授权与表单禁用状态可用 |
| 持久化 | 测试关闭数据库后重新打开，工作空间记录仍存在；不是整机故障恢复测试 |

浏览器已走通：创建空间、医护认领、填写随访、生成模板、确认摘要、关闭、刷新、患者填写、主动请求。另验证CSRF拒绝、跨患者拒绝、幂等重放、并发认领一成功一409、独立空间不可访问对方任务、重置不影响另一空间。

测试过程中发现并修复：数据库父目录未创建导致首次启动失败；Next请求origin标准化与Host不一致；手机表格内辅助文本导致页面溢出；授权开关需等待服务端成功后确认状态。开发依赖审计问题通过升级Vitest解决。

## 尚未验证

- CI运行、Node22实际运行、Windows/Linux设备兼容性。
- 真实PostgreSQL服务、多进程部署、worker队列、备份和故障恢复。
- 100人90日10并发负载门槛、专业可访问性审计、渗透测试。
- 真实设备授权、临床准确性、真实患者体验和医疗安全有效性。
- 完整Spec覆盖；当前实现差异见09-implementation-status.md。

截图均由纯合成案例生成，可公开；不包含医院附件或真实个人资料。浏览器测试启动新的隔离空间，不应依赖截图中的具体随机任务ID。
